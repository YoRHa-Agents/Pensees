# {{SESSION_SLUG}} — Pensees Mid-Result Review

- Session date: `{{SESSION_DATE}}`
- Reviewer: `{{REVIEWER_VERSION}}`
- Turn count: `{{TURN_COUNT}}`
- Generated at: `{{GENERATED_AT_ISO}}`

## 1. Composite Scores Summary

- Premature-detail: max = `{{PREMATURE_MAX}}`, mean = `{{PREMATURE_MEAN}}`, threshold = `{{PREMATURE_THRESHOLD}}`.
- Dead-end: max = `{{DEAD_END_MAX}}`, mean = `{{DEAD_END_MEAN}}`, threshold = `{{DEAD_END_THRESHOLD}}`.

_Section read: when `PREMATURE_MAX` ≥ `PREMATURE_THRESHOLD` or `DEAD_END_MAX` ≥ `DEAD_END_THRESHOLD`, expect at least one flagged turn in §3 and a non-`clean` value in §5._

## 2. Per-Turn Score Chart

The CLI substitutes ASCII art or a markdown table of rows
`(turn_id, composite_premature, composite_dead_end)`. ASCII only; no
embedded images, no SVG, no PNG, no base64 binary.

```
{{TURN_SCORE_CHART}}
```

## 3. Flagged Turns

{{FLAGGED_TURNS_LIST}}

_Example format (the CLI renders one bullet per flagged turn in exactly this shape):_

```
- Turn N | premature-detail | composite=0.XX | quote: "..." (≤80 chars)
- Turn M | dead-end | composite=0.YY | quote: "..." (≤80 chars)
```

Quote convention: the user-utterance quote is truncated at 80 characters
by the CLI; when the original is longer, the CLI appends `...` to mark
truncation. The 80-character budget is measured before the trailing `...`.

## 4. Top Contributing Signals

{{TOP_SIGNALS_TABLE}}

_The CLI emits a small table of the 7 base signals' total contributions
across the session, sorted descending by contribution._

## 5. Summary Verdict

Verdict: `{{VERDICT}}`

- `clean` — no turn crossed `PREMATURE_THRESHOLD` or `DEAD_END_THRESHOLD`.
- `flagged` — 1 to 3 turns crossed either threshold.
- `high-friction` — 4 or more turns crossed either threshold.

Rationale: {{VERDICT_RATIONALE}}

## 6. Recommendations

{{RECOMMENDATIONS_LIST}}

_When the verdict is `clean`, the CLI emits the single bullet
`- (no recommendations)` so this section is never empty._

## 7. Provenance

Generated by `tools/pensees_review.py` from `{{TURNS_JSONL_PATH}}`. Schema: `{{SCHEMA_VERSION}}`. Generated at `{{GENERATED_AT_ISO}}`.

See also: skill/references/intermediate-result-schema.md (the data contract this template renders).
